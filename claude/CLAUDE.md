# Local Claude Settings

## Git Branches

When creating new branches, prefix the branch name with `alejandro/`.

## Pull Request Creation

When creating pull requests, read and follow the template in `.github/pull_request_template.md`.
