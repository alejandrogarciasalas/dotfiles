- defaults:
    shell:
      stdout: true
      stderr: true

# TODO: add anything else here?
- clean: ["~"]

- shell:
  - [sh zsh/install.sh, Installing and changing the default shell to zsh]
  - [sh brew/install.sh, Installing Homebrew and Brewfile]

- link:
  # zsh
  ~/.zshrc: zsh/zshrc.zsh
  # git
  ~/.gitconfig: git/gitconfig
  ~/.gitignore_global: git/gitignore_global
  # Sublime Text # TODO: copy over files
  # "~/Library/Application Support/Sublime Text 3/Packages/User": sublime
  # VSCode # TODO: copy over files
  # ~/Library/Application Support/Code/User/settings.json: vscode/settings.json
  # ~/Library/Application Support/Code/User/keybindings.json: vscode/keybindings.json
