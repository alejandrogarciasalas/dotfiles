# FIX: brew script won't find brewfile because of relative paths stuff
sh ../brew/install.sh # Installing Homebrew and Brewfile
sh ../macos/install.sh # setting up MacOS defaults
sh ../iterm/install.sh # import iTerm profile
